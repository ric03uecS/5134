language: node_js

node_js:
  - 0.12
  - 0.10

build:
  ci:
    - echo "Hello, World!"
    - echo "This is master!"
    - echo "This is ??????"
    - echo "Hello, World!!"
    - echo "This is !!!!!"

integrations:
  hub:
    - integrationName: dockerhub
      type: docker
