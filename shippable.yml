language: node_js
node_js:
  - 0.10
  - 0.12
build:
  ci:
    - docker ps
    - echo "Hello, World!"
    - echo "This is master!"
    - echo "This is ???"
    - n=$((0)); while [ $(($n < 100000)) == 1 ]; do n=$(($n + 1)); "echo" "$n"; done
