language: node_js

build:
  ci:
    - docker ps
    - echo "Hello, World!"
    - echo "This is master!"
    - echo "This is ??????"
    - echo "Hello, World!!!!"
    - ls /shippableci
    #- cat /shippableci/onstartjobenvs/*
integrations:
    notifications:   
        - integrationName: himanshu-webhook
          type: webhook
          recipients:
            - '#shippable'
          payload:
            - component=Shippable
          on_start: always
          on_success: always
          on_failure: always
          on_pull_request: always
