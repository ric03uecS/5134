language: node_js

build:
  cache: true
  ci:
    - docker ps
    - echo "Hello, World!"
    - echo "This is master!"
    - echo "This is ??????"
    - echo "Hello, World!!!!"
    - ls /shippableci
    - docker images
