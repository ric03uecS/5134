language: node_js

build:
  ci:
    - echo "Hello, World!"
    - echo "This is master!"
    - echo "This is ??????"
    - echo "Hello, World!!!!"
    - docker images
    - cat /tmp/cexec/message.json
    - exit 1
