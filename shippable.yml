language: node_js

node_js:
  - 0.12

build:
  ci:
    - echo "Hello, World!"
    - echo "This is master!"
    - echo "This is ??????"
    - echo "Hello, World!!"
    - echo "This is !!!!!"
    - exit 1

integrations:
  hub:
    - integrationName: dockerhub
      type: docker
  notifications:
#    - integrationName: triggerResource
#      type: webhook
#      payload:
#        - versionName=$BRANCH.$SHIPPABLE_BUILD_NUMBER
#      on_success: always
#      on_failure: never
#      on_start: never
    - integrationName: slack-himanshu
      recipients:
        - '@himanshu'
      type: slack
      on_start: always
      on_failure: always
      on_success: always
