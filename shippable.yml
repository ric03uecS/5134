language: node_js

env:
  global:
    - FOO:&&

build:
  ci:
    - docker ps
    - echo "Hello, World!"
    - echo "This is master!"
    - echo "This is ??????"
    - echo "Hello, World!!!!"
    - ls /shippableci
    - docker images
integrations:
    notifications:   
        - integrationName: himanshu-webhook
          type: webhook
          recipients:
            - '#shippable'
          payload:
            - component=Shippable
          on_start: always
          on_success: always
          on_failure: always
          on_pull_request: always
