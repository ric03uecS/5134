language: node_js

build:
  ci:
    - docker ps
    - echo "Hello, World!"
    - echo "This is master!"
    - echo "This is ??????"
    - sleep 5m
    - echo "Hello, World!!!!"
    - ls /shippableci
    - docker images
